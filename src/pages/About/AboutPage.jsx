export default function AboutPage() {
  return (
    <section className="panel about-card">
      <h2>About This App</h2>
      <p>
        This finance tracker is a React 18 single-page application built with Vite. It uses modular reusable
        components, custom hooks, and route-based pages for a scalable architecture.
      </p>
      <p>
        Features include separate income and expense flows, analytics charts, search/filter/sort, theme and currency
        preferences, favorites, and CSV export.
      </p>
    </section>
  );
}
